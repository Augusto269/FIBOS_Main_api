{
    "type": "mongodb",
    "host": "${MONGO_HOST}",
    "port": "${MONGO_PORT}",
    "database": "${MONGO_DB}",
    "useUnifiedTopology": true,
    "synchronize": true,
    "logging": false,
    "entities": [
      "src/**/*.entity.ts"
    ],
    "cli": {
      "entitiesDir": "src"
    }
  }
  